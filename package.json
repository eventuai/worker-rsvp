{
  "name": "worker-rsvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public RSVP website backed by 0xCMS — renders EDM-driven RSVP forms from the published database.",
  "scripts": {
    "dev": "wrangler dev --local --persist-to ../cms/.wrangler/state",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "liquidjs": "^10.27.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260603.1",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.107.0"
  }
}
